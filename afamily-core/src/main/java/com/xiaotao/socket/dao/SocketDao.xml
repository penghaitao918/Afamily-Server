<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD iBatis Mapper 3.0 //EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaotao.socket.dao.SocketDao">

    <select id="loginCheck" resultType="com.xiaotao.socket.model.SocketLog">
        SELECT * FROM student_user WHERE student_id=#{userName}
    </select>

</mapper>